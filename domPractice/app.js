// DOM Manipulation

// Selecting Elements in the DOM

// By ID
const title =  document.getElementById('headin'); 

// By class name

const listItems = document.getElementsByClassName('list-items');

// By Tag Name

const listItems = document.getElementsByTagName('li');

// querySelector()
// grabs the first element with that selector
// use these bc it's simpler
const container = document.querySelector('div');

// querySelectorAll()
// grabs all of the elements with that selector
const container = document.querySelectorAll('div')